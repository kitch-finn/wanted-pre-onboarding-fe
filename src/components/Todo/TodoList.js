import React, { useState } from 'react';

function TodoList(todo) {
  {
    console.log(todo.todo);
  }
  <>
    <ul>
      {/* {todo.todo.map((todo) => {
        <>
          <div>{todo.todo}</div>
          <button>수정</button>
          <button>삭제</button>
          <button>완료</button>
        </>;
      })} */}
    </ul>
  </>;
}

export default TodoList;
